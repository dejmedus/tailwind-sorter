@page
@model IndexModel
@inject MarkdownBlog BlogPosts
@{
    ViewData["Title"] = "Home page";
}

<div class="mx-auto px-5 container">
    <section class="flex md:flex-row flex-col md:justify-between items-center mt-16 mb-16 md:mb-12">
        <h1 class="md:pr-8 font-bold text-6xl md:text-8xl leading-tight tracking-tighter">Razor Tailwind</h1>
        <div class="flex flex-col">
            <h4 class="mt-5 text-lg">
                A Modern Razor Pages <a href="https://tailwindcss.com/" class="@Css.LinkUnderline">Tailwind</a> App with
                <a href="https://vuejs.org" class="@Css.LinkUnderline">Vue.js</a> &amp; Markdown
            </h4>
            <div class="flex justify-between items-center">
                <div id="hello-api" data-component="HelloApi" data-props="{ value: 'Tailwind' }" class="mt-2"></div>
                @Html.SrcComponent("HelloApi.mjs", size: "w-8 h-8 mt-2")
            </div>
        </div>
    </section>
</div>


<div class="mx-auto px-5 container">
    @{
        var posts = BlogPosts.GetPosts().Take(3).ToList();
        var post = posts.FirstOrDefault();
        if (post != null)
        {
            <section>
                <div class="mb-8 md:mb-16">
                    <div class="sm:mx-0">
                        <a aria-label="@post.Title" href="@BlogPosts.GetPostLink(post)">
                            <img src="@post.Image" alt="Cover Image for @post.Title" class="shadow-sm hover:shadow-2xl transition-shadow duration-200">
                        </a>
                    </div>
                </div>
                <div class="md:gap-x-16 lg:gap-x-8 md:grid md:grid-cols-2 mb-20 md:mb-28">
                    <div>
                        <h3 class="mb-4 text-4xl lg:text-6xl leading-tight">
                            <a class="hover:underline" href="@BlogPosts.GetPostLink(post)">@post.Title</a>
                        </h3>
                        <div class="mb-4 md:mb-0 text-lg">
                            <time datetime="@BlogPosts.GetDateTimestamp(post.Date)">@BlogPosts.GetDateLabel(post.Date)</time>
                        </div>
                    </div>
                    <div>
                        <p class="mb-4 text-lg leading-relaxed">@post.Summary</p>
                        <div class="flex items-center">
                            <img src="@BlogPosts.GetAuthorProfileUrl(post.Author)" class="mr-4 rounded-full w-12 h-12" alt="Author">
                            <div class="font-bold text-xl">@post.Author</div>
                        </div>
                    </div>
                </div>
            </section>
        }

        void RenderPost(MarkdownFileInfo post)
        {
            <div>
                <div class="mb-5">
                    <div class="sm:mx-0">
                        <a aria-label="@post.Title" href="@BlogPosts.GetPostLink(post)">
                            <img src="@post.Image" alt="Cover Image for @post.Title" class="shadow-sm hover:shadow-2xl transition-shadow duration-200">
                        </a>
                    </div>
                </div>
                <h3 class="mb-3 text-3xl leading-snug">
                    <a class="hover:underline" href="@BlogPosts.GetPostLink(post)">@post.Title</a>
                </h3>
                <div class="mb-4 text-lg">
                    <time datetime="@BlogPosts.GetDateTimestamp(post.Date)">@BlogPosts.GetDateLabel(post.Date)</time>
                </div>
                <p class="mb-4 text-lg leading-relaxed">@post.Summary</p>
                <div class="flex items-center">
                    <img src="@BlogPosts.GetAuthorProfileUrl(post.Author)" class="mr-4 rounded-full w-12 h-12" alt="@post.Author">
                    <div class="font-bold text-xl">@post.Author</div>
                </div>
            </div>        
        }

        var morePosts = posts.Skip(1).ToList();
        if (morePosts.Count == 2)
        {
            <section>
                <h2 class="mb-8 font-bold text-6xl md:text-7xl leading-tight tracking-tighter">More Stories</h2>
                <div class="gap-y-20 md:gap-x-16 md:gap-y-32 lg:gap-x-32 grid grid-cols-1 md:grid-cols-2 mb-32">
                    @{ RenderPost(morePosts[0]); }
                    @{ RenderPost(morePosts[1]); }
                </div>
            </section>        
        }
    }
</div>

<section>
    <h2 class="pt-8 md:pr-8 font-bold text-4xl md:text-6xl text-center leading-tight tracking-tighter whitespace-nowrap">
        Getting Started
    </h2>
    <div class="mb-8 md:mb-16">
        <div class="flex justify-evenly sm:mx-0 sm:px-4 sm:py-8">
            <div data-component="pages/GettingStarted.mjs" data-props="{ template:'razor' }"></div>
        </div>
        <div class="flex justify-center">
            @Html.SrcPageMjs("GettingStarted.mjs")
        </div>
    </div>
</section>